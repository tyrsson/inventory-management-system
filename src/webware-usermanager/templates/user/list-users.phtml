<?php

declare(strict_types=1);

use Laminas\Permissions\Acl\Role\RoleInterface;

/**
 * @var \Webware\UserManager\UserInterface[] $users
 */
$this->headTitle('Users');
?>

<nav aria-label="breadcrumb" class="mb-2">
    <ol class="breadcrumb small mb-0">
        <li class="breadcrumb-item"><a href="<?= $this->escapeHtmlAttr($this->userAdminUrl('')) ?>">User Manager</a></li>
        <li class="breadcrumb-item active" aria-current="page">Users</li>
    </ol>
</nav>

<div class="d-flex align-items-center justify-content-between mb-4">
    <div>
        <div class="d-flex align-items-center gap-2 mb-1">
            <i class="bi bi-people-fill text-primary fs-5"></i>
            <h1 class="h4 fw-bold mb-0">Users</h1>
        </div>
        <p class="text-secondary small mb-0">Manage user accounts, roles, and active status.</p>
    </div>
    <a href="<?= $this->escapeHtmlAttr($this->userAdminUrl('create')) ?>"
       class="btn btn-primary btn-sm">
        <i class="bi bi-plus-lg me-1"></i> Add User
    </a>
</div>

<div class="card">
    <div class="card-header py-2 d-flex align-items-center gap-2">
        <i class="bi bi-people-fill text-secondary"></i>
        <span class="small fw-semibold">All Users</span>
        <span class="badge bg-body-tertiary border text-secondary ms-1"><?= count($users) ?></span>
    </div>
    <div class="table-responsive">
        <table class="table table-hover table-sm mb-0 align-middle">
            <thead class="table-dark">
                <tr>
                    <th class="ims-col-name">Name</th>
                    <th class="ims-col-email">Email</th>
                    <th class="ims-col-role">Role</th>
                    <th class="text-center ims-col-status">Status</th>
                    <th class="ims-col-actions"></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($users as $user): ?>
                    <tr<?= $user->active ? '' : ' class=""' ?>>
                        <td>
                            <span class="fw-semibold<?= $user->active ? '' : ' text-secondary' ?>">
                                <?= $this->escapeHtml($user->firstName . ' ' . $user->lastName) ?>
                            </span>
                        </td>
                        <td class="small text-secondary">
                            <?= $this->escapeHtml($user->email) ?>
                        </td>
                        <td>
                            <?php $roles = $user->getRoles(); ?>
                            <?php if ($roles !== null && $roles !== []): ?>
                                <?php foreach ($roles as $role): ?>
                                    <?php $roleName = $role instanceof RoleInterface ? $role->getRoleId() : (string) $role; ?>
                                    <span class="badge bg-body-tertiary border text-secondary"><?= $this->escapeHtml($roleName) ?></span>
                                <?php endforeach; ?>
                            <?php else: ?>
                                <span class="text-secondary fst-italic small">—</span>
                            <?php endif; ?>
                        </td>
                        <td class="text-center">
                            <?php if ($user->active): ?>
                                <span class="badge bg-success-subtle text-success-emphasis border border-success-subtle">Active</span>
                            <?php else: ?>
                                <span class="badge bg-danger-subtle text-danger-emphasis border border-danger-subtle">Inactive</span>
                            <?php endif; ?>
                        </td>
                        <td class="text-end">
                            <button
                                class="btn btn-sm btn-outline-secondary py-0 px-2"
                                hx-get="<?= $this->escapeHtmlAttr($this->userAdminUrl('edit.modal', ['id' => $user->id])) ?>"
                                hx-target="#sharedModalDialog"
                                hx-swap="innerHTML"
                                hx-push-url="false"
                                hx-on::after-request="if(event.detail.successful) bootstrap.Modal.getOrCreateInstance(document.getElementById('sharedModal')).show()"
                                title="Edit user">
                                <i class="bi bi-pencil"></i>
                            </button>
                            <button
                                class="btn btn-sm <?= $user->active ? 'btn-outline-warning' : 'btn-outline-success' ?> py-0 px-2 ms-1"
                                hx-post="<?= $this->escapeHtmlAttr($this->userAdminUrl('toggle.update', ['id' => $user->id])) ?>"
                                title="<?= $user->active ? 'Deactivate user' : 'Reactivate user' ?>">
                                <i class="bi bi-<?= $user->active ? 'toggle-on' : 'toggle-off' ?>"></i>
                            </button>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
    <div class="card-footer py-2 text-secondary small">
        Deactivated users cannot sign in. Their data and history are preserved.
    </div>
</div>
