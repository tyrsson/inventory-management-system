# Outstanding Items — 2026-05-22

> **Note:** Items marked *(likely dropped)* reflect code that was written speculatively
> and may be removed rather than implemented. Retain until the decision is final.

## Active Branch

- **Ownership assertion aggregates** (`acl-ownership-assertion-aggregates`)
  — in progress, not yet merged to `0.1.x`

## ACL — Code Incomplete *(likely dropped)*

- **ACL admin command handlers are all no-ops**
  `ProtectRouteHandler`, `SaveRuleHandler`, `DeleteRuleHandler`, `UpdateRuleTypeHandler`,
  `SaveRoleHandler`, `DeleteRoleHandler`, `SaveAssertionHandler`, `DeleteAssertionHandler`
  — each has `@todo Implement config-driven * via ConfigSaveEvent` and returns
  `CommandStatus::Success` without doing anything. `ConfigSaveEvent` pipeline is not wired.

- **`AclBuilder::buildFromArrays()` not wired**
  — `@todo Wire config array data into buildFromArrays()`. The config-driven static ACL
  build path exists in code but the data is never passed to it.

## ACL — Functionality Gap

- **`RoleListHandler` POST not implemented**
  — `TODO: implement POST (task 2.7)` — GET works, POST handler body is a stub.

## Documentation — Still to Review

- `src/webware-acl/docs/acl-build-pipeline.md` — not yet audited for stale content
  (may reference deleted classes or old pipeline order)
- `src/webware-acl/docs/admin-ui-workflows.md` — same, not yet reviewed

## Documentation — Known Quick Fixes

- `docs/module/user/README.md` intro still references `src/User/`
  — should be `src/webware-usermanager/`

## Config — To Verify

- **`ForbiddenHandler` `forbidden_redirect` default is `'/'`**
  — if an authenticated-but-denied user hits a restricted route, they are redirected to `/`.
  Confirm whether a module-level config key should be set to something more useful
  (e.g. the store dashboard). `login_path` override is correctly set in
  `webware-usermanager/src/ConfigProvider.php` line 140.

## Phase 2 / Deferred (not actionable yet)

- Barcode scanning via ZXing-js (documented in must-have, deferred)
- Delivery Route Builder + geocoding
- `mezzio-async` reintegration when TrueAsync extension stabilises

---

## Session Context (2026-05-22)

This session completed a full documentation audit and rewrite:

| File | Status |
|---|---|
| `docs/module/user/login.md` | Rewritten — PhpSession/AuthenticationMiddleware removed |
| `src/webware-usermanager/docs/identity-flow.md` | Renamed (was `-bugs`), rewritten |
| `src/webware-acl/docs/authorization-middleware.md` | Fixed — correct class name, fail-closed language |
| `src/webware-acl/docs/integration-guide.md` | Fixed — same corrections |
| `.github/skills/webware-acl/SKILL.md` | Major fix — was completely inverted (said `AuthorizationMiddleware` deleted, `AuthorizingDispatchMiddleware` current) |
| `.github/skills/webware-acl-ownership-command/SKILL.md` | Fixed |
| `docs/module/user/README.md` | Fixed — pipeline diagram, config keys |
| `docs/Project_Architecture_Blueprint.md` | Fixed — auth stack table, removed `LoginCommand`/PhpSession |

**Key facts confirmed from source:**
- `AuthorizingDispatchMiddleware` — **does not exist**, deleted in a prior refactor
- `AuthorizationMiddleware` — the real current class, in `Middleware/` directory
- `Acl::isAllowedRoute()` — **FAIL CLOSED**: returns `false` for unregistered routes
- `login_path` override: `webware-usermanager/src/ConfigProvider.php` line 140
