# ACL Work Todo — 2026-05-25

## Immediate (Next Session)

- [ ] **Fix duplicate role parents in `acl.global.php`**
  Investigate `SaveRuleHandler` — it likely needs to use `replace: true` with the full
  assembled config rather than a delta merge. Confirm that `UpdateRuleTypeHandler`
  (already using `replace: true`) does not re-introduce duplicates on subsequent writes.

- [ ] **Test rule update (allow↔deny toggle) from the Protected Routes dropdown**
  Adding a rule via the wizard was confirmed working. The next test is the PATCH flow:
  open the "Rules N" dropdown on a protected route, toggle an existing rule's type
  (allow → deny or vice versa), and confirm:
  - `ProcessRuleMiddleware::processPatch` dispatches `UpdateRuleTypeCommand` correctly
  - Toast "Rule updated." appears
  - Overview refreshes inline (no full page reload)
  - `acl.global.php` is rewritten with `replace: true` — old rule removed, new type written
  - Direct child roles get an explicit preservation rule where appropriate

## Short Term

- [ ] **Route-mapping deletion sweep**
  Delete the entire route-mapping subsystem — no longer needed now that routes are
  resources managed by the wizard:
  - `SaveRouteMappingCommand` / `SaveRouteMappingHandler` / factory
  - `DeleteRouteMappingCommand` / `DeleteRouteMappingHandler` / factory
  - `RouteMapManagerHandler` + factory + template
  - Related middleware
  - `RegisterManifestRouteMappingsListener` (`ims-manifest`)
  - `AclRepository` / `AclRepositoryInterface` route-mapping methods
  - `PrivilegeInterface` + `PrivilegeTest` (nothing left using privileges)

- [ ] **`webware-admin` resources/allow config**
  `ConfigProvider::getAclConfig()` currently only declares roles. Add `resources` and
  `allow` entries for all `webware.admin.*` routes so they are protected correctly.

- [ ] **IMS store ownership layer**
  1. Create `StoreProprietaryInterface` in `src/ims-store/src/Acl/` — show before writing
  2. Update `StoreOwnedResourceInterface` to extend it — show before writing
  3. Update `StoreOwnedResourceAssertion` to use `instanceof` on both sides — show before writing
  4. IMS `UserInterface` extension + `SingleRoleUserProxy` subclass for `getStoreId()`

## Tests (deferred)

- [ ] `SingleRoleUserProxy`
- [ ] `UserRoleIterator`
- [ ] `AuthorizationMiddleware`
- [ ] `UpdateRuleTypeHandler` — cascade logic (direct children only)
- [ ] End-to-end assertion: `StoreOwnedResourceAssertion` with live `Member` session
