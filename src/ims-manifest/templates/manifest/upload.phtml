<?php
/**
 * Manifest CSV upload page.
 * Errors are delivered via SystemMessenger toasts — no inline error variable needed.
 */
$this->headTitle('Upload Manifest');
?>

<!-- Page header -->
<div class="d-flex align-items-center justify-content-between mb-3">
    <div>
        <h1 class="h4 fw-bold mb-0">Upload Manifest</h1>
        <p class="text-secondary small mb-0">
            <a href="<?= $this->manifestUrl('list') ?>" class="text-secondary text-decoration-none">
                <i class="bi bi-arrow-left me-1"></i>Back to Manifests
            </a>
        </p>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-12 col-md-8 col-lg-6">
        <div class="card shadow-sm">
            <div class="card-header">
                <h6 class="mb-0"><i class="bi bi-file-earmark-spreadsheet me-2"></i>DC Truck Manifest CSV</h6>
            </div>
            <div class="card-body">
                <form method="post"
                      action="<?= $this->manifestUrl('upload.store') ?>"
                      enctype="multipart/form-data"
                      novalidate>

                    <div class="mb-4">
                        <label for="manifest_csv" class="form-label fw-semibold">
                            Manifest CSV File <span class="text-danger">*</span>
                        </label>
                        <input type="file"
                               class="form-control"
                               id="manifest_csv"
                               name="manifest_csv"
                               accept=".csv,text/csv,text/plain"
                               required>
                        <div class="form-text">
                            Select the CSV file exported from the DC system
                            (e.g. <em>Dist DC Truck Manifest - Live.csv</em>).
                        </div>
                    </div>

                    <div class="mb-4">
                        <label for="received_date" class="form-label fw-semibold">
                            Received Date Override
                        </label>
                        <input type="date"
                               class="form-control"
                               id="received_date"
                               name="received_date">
                        <div class="form-text">
                            Defaults to the date in the consignment header. Override only if
                            the truck arrived on a different calendar day.
                        </div>
                    </div>

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-upload me-1"></i>Import Manifest
                        </button>
                        <a href="<?= $this->manifestUrl('list') ?>" class="btn btn-outline-secondary">
                            Cancel
                        </a>
                    </div>

                </form>
            </div>
        </div>

        <p class="text-secondary small mt-3">
            <i class="bi bi-info-circle me-1"></i>
            Customer allocation rows are automatically skipped. Unknown SKUs and major codes
            will be added to the catalogue and can be updated later from Settings.
        </p>
    </div>
</div>
